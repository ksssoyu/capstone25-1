import { Box, styled } from '@mui/material';

export const ModalLabelContainer = styled(Box)(() => ({
  display: 'flex',
  justifyContent: 'center',
  flexDirection: 'column',
  padding: '20px 0px',
  alignItems: 'center',
}));
